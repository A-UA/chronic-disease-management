chore(backend): 源码大清洗，代码规范化及遗留脚本清理

- 自动化格式化：使用 ruff 全局执行代码格式化，解决引号、空行与长尾空格问题
- 静态错误修复：修复了乱序导入 (I001)、解决冗余及无用导入 (F401)、修剪未利用的局部变量 (F841)
- 架构遗留清理：彻底删除了已废弃的历史脚手架工具 (`sync_permissions.py` 和 `cleanup_mixins.py`)
- ORM 模型修复：在 `app/models/__init__.py` 中补全了缺失输出的元数据列表（`__all__`）
- 整体消除了多处框架兼容型隐患，代码库维持三层架构的高健康度要求
